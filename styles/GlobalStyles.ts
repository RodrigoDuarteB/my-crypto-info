import { StyleSheet } from "react-native";

export default StyleSheet.create({
    completelyCenter: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    verticalCenter: {
        justifyContent: 'center'
    },
    horizontalCenter: {
        alignItems: 'center'
    }
})